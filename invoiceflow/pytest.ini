[pytest]
# The suite is named tests_*.py (not pytest's default test_*.py), so a bare
# `python -m pytest` from this directory finds every file.
python_files = tests_*.py
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
