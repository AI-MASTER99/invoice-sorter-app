fastapi==0.115.5
uvicorn[standard]==0.32.1
# 0.0.18+ fixes CVE-2024-53981 (DoS via malformed multipart boundaries —
# live-reachable through the invoice upload endpoint). Drop-in upgrade.
python-multipart==0.0.20
anthropic==0.40.0
openpyxl==3.1.5
# Hold at 0.27.x: anthropic + supabase both constrain httpx; 0.28 removed
# shortcuts they relied on. Bump only in lockstep with those two.
httpx==0.27.2
python-dotenv==1.0.1
aiofiles==24.1.0
pdfplumber==0.11.4
# passlib 1.7.4 is unmaintained; it reads bcrypt.__about__ which was
# removed in bcrypt 4.1+, so bcrypt is pinned to the last compatible
# release. Migrating _pwd_ctx to the bcrypt lib directly is the long-term
# fix (existing $2b$ hashes verify unchanged).
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
itsdangerous==2.2.0
supabase==2.28.3
# Phase B: HS256 JWT minting for per-request user-scoped Supabase client.
# No `[crypto]` extra — HS256 lives in pyjwt's core, no Rust toolchain
# pulled into the build.
pyjwt>=2.8,<3
